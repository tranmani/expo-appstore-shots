/**
 * expo-router's native tabs render a real UITabBar on iOS — the OS draws it, not
 * your app, so a browser has nothing to render. The bar in the screenshots is
 * redrawn from `config.tabBar` instead (see ../TabBar.tsx). This stub only keeps
 * the imports resolving.
 */
export const NativeTabs = () => null
NativeTabs.Trigger = () => null
export const Icon = () => null
export const Label = () => null
export const Badge = () => null
export default NativeTabs
