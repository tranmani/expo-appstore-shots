/**
 * A stand-in for lucide, used by one test to prove that `config.stubs` beats the
 * tool's own alias for the same package. The marker is deliberately absurd: if
 * it is in the bundle, the user's config won the argument.
 */
export const USER_STUB_WON = true
export const __shotsLucideMissing = true
